object Main {
  def main(args: Array[String]): Unit = { // Specify return type as Unit (equivalent to void in Java)
    var p = 10
    var q = 30
    var sum = p + q
    println("The sum of p and q is: " + sum)
  }
}
