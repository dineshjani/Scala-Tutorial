/*
class Identifier{
    var a: Int = 20
    //first letter of data type should be capital beacause data type consider as a object in scala
    // every object first letter should be capital
    // Variable name should be in lower case
}
object Main {
    def main(args: Array[String]) {
        var ob = new Identifier();
        println(ob.a)
    }
}
*/